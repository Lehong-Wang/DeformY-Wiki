---
abstract: "Sim-campaign plan independently re-derived (v3.1) and handed over:
           base method = hindsight-relabeled conditional flow matching over
           smooth basis params + sim-verified best-of-N; MMP++ demoted to
           shootout arm; fresh-agent review (sound-with-fixes) and field scan
           (SOTA-consistent, two novelty deltas open) integrated; handover
           docs written at repo root."
---

# 2026-07-28 (session spanning 2026-07-24 → 07-28)

## What happened

- Experiment plan re-derived from physics + primary literature per user
  instruction ("don't trust the wiki plan"), through several user-driven
  pivots: direction goal made first-class (5D goal space; the honest analysis
  is that reachable-direction *coverage*, not precision, is the open physics
  question — in the smooth-swing regime (~2 m curvature, user's correction)
  timing jitter costs only ~1–3° of direction); smoothness question resolved
  (arm smoothness by construction in the curve layer; tip smoothness via
  filter + cost + verification — no architecture can guarantee it, including
  MMP++'s latent); PPO-in-3D failure diagnosed as exploration/sparse-
  supervision, fixed by per-timestep hindsight relabeling rather than by a
  "more advanced" method.
- Base method decided and recorded (see DECISIONS 2026-07-28); user requested
  high-level plans only — code scaffolding intentionally NOT written (a
  started scaffold was deleted on user instruction mid-session).
- Independent fresh-agent review: **sound-with-fixes** — critical fixes
  adopted into plan v3.1: evaluation de-circularized (rollout-level holdout,
  pre-registered task box, uniform eval sampling, distance-to-training
  curves), verifier-mismatch ranking test added (THE load-bearing assumption
  for real deployment), pass-time canonicalization of hindsight pairs, speed
  stratification everywhere, structured sweep seeding + iterated resweep,
  arm-tracking envelope.
- Field scan: method is 2026-SOTA-consistent (DA-MMP validates the recipe);
  novelty deltas and new anchors recorded in NOTES.md.

## Handover artifacts (repo root)

- `rope_swing_sim_experiment_plan.md` — staged campaign v3.1 (stages 0/A/B/C/D
  with exit criteria, review changelog §8, field-scan addendum §9).
- `rope_swing_base_experiment.md` — base-experiment reasoning chain, go/no-go
  decision logic, both review reports condensed.
- `rope_swing_decisions.md` — canonical ledger updated with 07-24/25/28
  entries.

## Open threads

- Implement the base experiment on the remote sim server (six components,
  ~1–2k lines glue; env adapter contract is specified in the base-experiment
  doc — tip *state* velocities required, not finite-differenced positions).
- Per-goal compute ruling awaiting user confirmation (DECISIONS, proposed).
- Pre-register the task box (target volume × direction set × min speed)
  BEFORE the first sweep — this is a review-mandated ordering constraint.
- Watch DMMP/DA-MMP for code release; re-run the novelty check near
  submission.
